/dist
.env
.DS_Store
/uploads
# Archivos subidos por el Admin (packshots). En prod viven en el Railway Volume
# montado en .medusa/server/static, no en git.
/static
/node_modules
yarn-error.log

.idea

coverage

!src/**

# El !src/** de arriba re-incluye todo bajo src/, y eso "des-ignoraba" la basura
# de macOS ahí dentro. La volvemos a ignorar (en .gitignore gana la última regla).
**/.DS_Store

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa