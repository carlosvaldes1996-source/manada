# CURRENT STATE — Dónde estamos AHORA

> **📋 Metadata**
> | Campo | Valor |
> |---|---|
> | **Purpose** | Foto del estado actual: qué es real, qué frentes están abiertos y cuál es el siguiente paso. Se **reescribe** al cerrar cada hito (no se apila; la narración histórica vive en `DECISIONS.md`). |
> | **Owner** | Carlos (fundador) · Claude |
> | **Status** | 🟢 Vivo |
> | **Last Updated** | 2026-08-05 |
> | **Depends On** | DECISIONS.md, ROADMAP.md |
> | **Supersedes** | `history/05-bitacora-avances-2026-07.md` (versión-bitácora archivada) |
> | **Source of Truth** | ✅ del *estado actual y el siguiente paso*. Único dueño del estado: ningún otro doc lo repite. |

## Fase activa

**Fase 5 — MVP (D22, MVP-first)**, en su tramo final, con dos realidades en paralelo:

1. **El flujo propio del MVP está CERRADO y endurecido** (D23–D29): catálogo, carrito, checkout→pago con **Flow** (D58, reemplaza el pago manual de D24), cuentas/sesión, buscador y regla de envío — todo real sobre Medusa v2, sin datos demo en ningún flujo real (D33). La suscripción se vende como **compra única** (`SUBSCRIPTIONS_ENABLED=false`, D29).
2. **El "moat diferido" de D22 quedó parcialmente superado:** el **perfil de mascota ya es real y persistido** — módulo custom `pet` en el backend (D34), anticipación anclada a la compra (D35), edición real (D37/D38) y separación comprar≠definir-qué-come (D39). Lo que sigue diferido post-tracción es la **suscripción recurrente** y el motor de anticipación completo.

**Etapa vigente declarada por Carlos (D38):** prioridad a **consistencia visual, UX y sensación de producto terminado** por sobre agregar capacidades. El backend está consolidado.

## ✅ Infraestructura de producción — EN VIVO (2026-07-16, D30)

> **La tienda está pública y operativa.** Backend Medusa en **Railway** + frontend Next.js en **Vercel** con dominio propio. Cierra la deuda de infra de D25/D27/D29 (topología acordada: un solo servicio `shared`, build nativo pnpm sin Docker).

- **Backend:** `https://manadabackend-production.up.railway.app` — Railway (proyecto `creative-creation`, servicio `@manada/backend`). `/health` 200 · **Postgres + Redis** gestionados · **volumen** `@manada/backend-volume` en `/app/apps/backend/.medusa/server/static` (packshots persisten, sin S3). Build por `railway.json` (nativo, migraciones en `preDeployCommand`). **Build endurecido (D67):** el `buildCommand` ya **no** hace un 2º `pnpm install` en `.medusa/server` (era la causa del `ERR_PNPM_FETCH_429` intermitente: sin lockfile re-resolvía cientos de deps contra el registry); ahora usa un **symlink** al `node_modules` del workspace → cero registry, build verde y rápido (~5 min). Vars reales: `NODE_ENV=production`, `PORT=9000` (fix de un 502: Medusa quedaba en 8080), secrets aleatorios, CORS con `tumanada.cl`+`www`+vercel, `MEDUSA_BACKEND_URL`, `MEDUSA_WORKER_MODE=shared`.
- **Datos de prod (seed corrido una vez):** región Chile/CLP, sales channel, **publishable key** `pk_0fe6…`, 6 productos con precio, 2 opciones de envío, promo `ENVIO_GRATIS_30K` (gratis ≥ $30.000). **Admin:** `.../app` (carlosvaldes1996@gmail.com).
- **Frontend:** en vivo en **`https://www.tumanada.cl`** (apex → 308 a `www`, canónico), SSL OK, catálogo real hidratando, CORS OK. Vercel `manada-web` con las 2 env vars de prod.
- **Tracking (D46) en vivo:** GTM `GTM-P5RLWHJW` + GA4 `G-1JQM28SLWW` conectada dentro de GTM (6 eventos mapeados, publicado, verificado en Tiempo real). Se corrigió un `\n` colado en `NEXT_PUBLIC_GTM_ID` (limpiado en Vercel + guard `.trim()` en `lib/analytics/config.ts`). **Meta Pixel `1437594504862107` conectado también dentro de GTM (D53):** pixel base + `PageView` + 4 conversiones (AddToCart/InitiateCheckout/Purchase/ViewContent) mapeadas desde el mismo `dataLayer`; GA4 intacto; sin tocar código ni env var. **Verificado en Meta Test Events (2026-07-23):** PageView + AddToCart + InitiateCheckout llegando ("Procesado"); `Purchase`/`ViewContent` a confirmar con compra de prueba (mismo mecanismo → dispararán en la primera venta real).
- **Emails transaccionales (D45/D49) EN VIVO:** Resend con dominio `tumanada.cl` verificado + `RESEND_FROM=Manada <contacto@tumanada.cl>` + `STOREFRONT_URL=https://tumanada.cl` en Railway → envío real (bienvenida verificada punta a punta). El CTA de bienvenida lleva a `/cuenta/mascotas` (adapta en tiempo de clic). Vercel solo aporta DNS; el envío corre en el backend. **Añadidos los 5 correos del ciclo de suscripción (D57·R5) sobre la misma infra — verificados con `medusa build`, pendiente deploy a Railway.**
- **Pendiente (no bloquea, fast-follow):** **packshots** por Admin (hoy emoji) · **Search Console** (verificar con GTM + enviar sitemap) · **Flow** (D58): setear `FLOW_API_KEY`/`FLOW_SECRET_KEY`/`FLOW_API_URL=https://www.flow.cl/api` en Railway + E2E en sandbox. Detalle y runbook con resultados: `DEPLOYMENT.md`.

## Qué está construido y es real (fuentes de verdad)

- **Backend Medusa v2** (`apps/backend`, 2.16.0): catálogo administrable + `subscription_price` calculado (D23) · carrito/checkout→pago con **Flow** (D58; la orden se crea al confirmar el pago vía webhook+`getStatus`, reemplaza el pago manual de D24) · auth de cliente + direcciones + pedidos (D26) · regla única de envío `/store/shipping-policy` (D28; el **buscador ya no usa el `q` nativo** — la relevancia vive en `apps/web`, D69) · **módulo custom `pet`** `/store/pets` (D34) + subscriber `order.placed` (D35) + **emails transaccionales** (D45): Notification Module nativo + provider Resend (`src/modules/resend`) con 4 emails críticos (bienvenida, reset, compra, envío) sobre un layout/componentes reutilizables; **EN VIVO en producción (D49)** con dominio verificado y remitente propio (modo DEV local sigue logueando sin API key). Contratos: `API.md §5–§9` + email en `API.md` · modelo: `DATABASE.md §5–§8` · setup local: `apps/backend/DEV.md`.
- **Frontend Next.js** (`apps/web`): 100% sobre el backend real; datos demo solo en el hero de la landing (decisión de marca, D28) y el styleguide `/dev/*` (gateado en prod, D29). Arquitectura: `FRONTEND_ARCHITECTURE.md` · componentes: `COMPONENT_LIBRARY.md`.
- **PDP multi-formato (D48):** la ficha de producto conecta el **selector de formato/talla a las variantes reales** del catálogo (`VariantSelector`; ej. `royal-canin-mini-puppy` con 1/3/7.5 kg) — precio, $/kg, duración y `variantId` de carrito derivan de la variante elegida (default = primaria = formato recomendado). Los productos de una sola variante no muestran selector. Ficha rediseñada al boceto (nombre → descripción real → selector → cápsulas → Compra única). Suscripción sigue apagada (D29). Doc: `COMPONENT_LIBRARY §5`. **Aparte:** existe una **preview de diseño OCULTA** de la card "Plan Manada" (`plan-manada-preview.tsx`, gate `SHOW_PLAN_MANADA_PREVIEW=false`, CTA inerte) — material aprobado por Carlos para el bloque de suscripción futuro; no cambia comportamiento.
- **Catálogo multi-formato — carga (D50) + storefront (D51):** el Admin tiene un **widget "Formatos"** en la ficha de producto (`POST /admin/products/:id/formats`) que crea variantes en un paso —encapsula el flujo opción→valor→variante de Medusa v2 y reemplaza la "Default variant"— con lo que se cargó el **primer producto real** (Amity, 4/14 kg). En el storefront (D51), las cards con varios formatos muestran **"Varios formatos · desde"** el más barato, el **stock** aparece en la card solo como urgencia (≤5/agotado), el **"precio por kilo"** pasó a precio unitario bajo el precio + empujón "rinde más" al formato grande, y las **devoluciones** son honestas (fuera la "garantía de sabor"; los alimentos abiertos no se devuelven). Docs: `API.md §12`, `COMPONENT_LIBRARY`. **D50 probado en local, pendiente deploy a Railway.**
- **Packshots normalizados (D52):** todo packshot que llega de Medusa se **normaliza server-side** (`GET /api/packshot`, `jimp` —JS puro, sin binario nativo, corre en la función de Vercel donde `sharp` no cargaba—: aplana sobre blanco → recorta el borde → re-encuadra a un cuadrado con margen uniforme, producto ~88 %) y `ProductImage` está graduado a **`next/image`** con loader custom (`srcset` responsive, JPEG cacheado, cero CLS). Resultado: asset con **fondo blanco o transparente** se ve **igual de grande y consistente** sin editar cada imagen; los pozos de media pasan a **blanco**. Verificado punta a punta contra el dev server. Doc: `COMPONENT_LIBRARY` (ProductImage) · `DESIGN_SYSTEM §6`. Owner del código: `lib/media/packshot.ts` + `app/api/packshot/route.ts`. **Render evolucionado (D66):** `ProductImage` deja el loader custom → `/api/packshot` produce una **imagen base** (JPEG q90) y el **optimizer nativo de Next** hace `srcset` + **AVIF/WebP** (−81 % bytes por imagen de card, medido); + `priority` LCP en la 1ª fila. `sharp` vuelve a `apps/web` pero por el optimizer (no el import en ruta que falló; el normalizador sigue `jimp`).
- **Buscador con relevancia propia (D69):** la búsqueda dejó de depender del `q` nativo de Medusa y se resuelve en **`apps/web/src/lib/search/`** (dueño único) sobre el catálogo que ya se lee cacheado (D68). El `q` nativo **no ve la marca** (vive en `metadata`; 21 % del catálogo no la lleva en el título), **exige todos los términos** ("acana cachorro" → cero, el producto se llama "Acana Puppy") y **no ignora tildes**. Ahora: plegado único de texto, **léxico ES↔EN jerárquico** (`cachorro≡puppy≡kitten`; `salmón ⊂ pescado`, expandido solo en el producto), ranking por **cobertura**, **degradación** exacto→marca→concepto→parecidos→destacados (**nunca una pantalla vacía**) y corrector Damerau-Levenshtein que solo se adopta si mejora. **Autocompletado nuevo:** `/api/buscar` (< 2 KB, `s-maxage=300`) + `SearchSuggest` (debounce 140 ms, `AbortController`, caché de sesión, búsquedas recientes, ARIA combobox) muestra **hasta 6 productos abribles desde 2 caracteres, sin Enter**, en el header y en `/buscar`. Medido: 0,05–0,13 ms por búsqueda, **cero requests nuevos al backend**; 51 consultas probadas sin una sola pantalla vacía. `searchProducts()` eliminado de `lib/medusa`. Doc: `API.md §8.1`. **Límite conocido (de datos, no del motor):** "sin granos", seco/húmedo, sabor y tamaño de raza no están en metadata → degradan a destacados; el motor ya habla esos conceptos y se arreglan cargando el dato en el Admin (y llenando `description`, hoy vacía en todo el catálogo).
- **Funnel de adquisición** F1–F4 ✅ sobre catálogo real (O5, D33) — doc: `FUNNEL_TARGET.md`. **F4 reconciliado (D62): adopta el diseño validado de `cristobal-cambios` —una sola columna mobile-first, cierre con check de éxito + ración como prueba, tarjeta destacada con banda de color + badges (formato/"Dura X días"), alternativas en línea— SOBRE la suscripción real (`PlanManadaCard` + compra única, patrón de la PDP, D56). Conserva aviso de sobrepeso, funnel unificado (→`/carrito`) y guard de hidratación; responsive corregido (una columna, sin overflow móvil). Supersede la presentación de D44; sin tocar recomendador/cálculos/contratos/APIs/estado. Verificado `tsc`+`eslint`+smoke Playwright 16/16; pendiente validación visual de Carlos.** La recomendación (F4) corre sobre el **motor defendible** (D43): cálculo nutricional RER/MER + densidad calórica, puertas duras (nunca recomienda incompatible) vs. score de preferencia, explicación verificada. Determinístico, sin IA — doc: `RECOMMENDATION_ENGINE.md`. **Alta simplificada a 2 pasos (D54, rescate de `cristobal-cambios`):** el wizard `comenzar/*` agrupa especie+nombre+raza y etapa+peso —menos transiciones, se siente más rápido— con F3 (peso no bloqueante) y el feedback de ración intactos. **Catálogo OFICIAL de razas (D63):** 81 perros + 32 gatos, fuente única `lib/breeds.ts` con "Mestizo / Sin raza definida" + texto libre; **la raza no entra al motor** (reemplazo inocuo, verificado: 4 razas → misma ración) y `pesoRangoAdulto` (AKC/FCI) alimenta solo la estimación de peso F3 con fallback a buckets; **aviso de sobrepeso** advisory (`overweightSignal`, >+15% del máx típico, exime robustas/esbeltas). **Frecuencia de suscripción por defecto = 4 semanas (D64)** en PDP + recomendación (el usuario la conserva al cambiar).
- **Pet Experience** B1–B8 ✅ COMPLETA (B4 foto con andamio local honesto + B7 /cuenta manada-first cerrados en el Product Completion Pass, D41; B5/B6 persistidos vía `/store/pets`; **B8 Home logueada = centro de control**, D42: `PetStatusCard` con retrato + línea de tiempo del saco + "Plan de {nombre}" + recompra en dos taps + necesidades) — doc: `PET_EXPERIENCE_TARGET.md`. **Anticipación honesta** (D41): la cápsula invita a "Pedir de nuevo"; el reagendo/suscripción vuelven post-tracción. **`/cuenta` reorganizado en tabs (D54, rescate de `cristobal-cambios`):** Mi manada / Pedidos / **Mi perfil** (datos del humano: nombre/email/RUT + accesos a direcciones/tarjetas) — separa el perfil del humano del de la mascota (antes el humano no tenía casa propia). Dashboard (D42) intacto como home logueada; suscripción como card "Próximamente" (D29). **Navegación unificada (D61):** el header monta un **menú único de identidad** (`components/pet/account-menu.tsx`) que separa "Tu mascota" (perfil canónico `/cuenta/mascotas`) de "Tu cuenta" (`/cuenta`) — reemplaza el `PetSwitcher` con nombre navegable + el ícono de usuario suelto que hacían leer el nombre de la mascota como "mi perfil"; el logo se mantiene = Inicio.
- **Deploy (D30):** EN VIVO — backend en Railway (`manadabackend-production.up.railway.app`) + frontend en Vercel con dominio **`www.tumanada.cl`** (SSL). Doc: `DEPLOYMENT.md` (§0 estado + §4.1 runbook).
- **Performance del catálogo (D59 + D66):** Home/PLP/PDP pasan de `force-dynamic` a **ISR `revalidate=300`** → se sirven desde el edge sin round-trip a Railway por visita (verificado `next start`: MISS→HIT, `s-maxage=300, stale-while-revalidate`). El catálogo se cachea vía `unstable_cache` (`lib/medusa/catalog-cache.ts`, `server-only`) porque el `fetch` sin cachear del SDK anulaba el `revalidate`; **carrito/checkout leen en vivo** (precio/stock reales al pagar). **Imágenes (D66):** los packshots pasan por el **optimizer nativo de Next** (AVIF/WebP + resize; −81 % bytes por imagen de card) y la 1ª fila de la PLP carga con `priority` (LCP). Docs: D59, D66. **Aparte (fix UX móvil):** el wizard de alta (`comenzar/onboarding-wizard.tsx`) **persiste el borrador en `sessionStorage`** → un "atrás" accidental del navegador ya no borra lo llenado (se limpia al completar el alta).

## Frentes abiertos (en paralelo, cada uno en su chat/bloque)

| Frente | Estado | Siguiente acción | Referencia |
|---|---|---|---|
| **Infra de producción** | 🟢 **EN VIVO** (D30) | Backend Railway + frontend Vercel + `tumanada.cl` operativos. Fast-follow: Resend, packshots, Search Console | D30 · `DEPLOYMENT.md` |
| **Flow — integración por etapas** | 🔒 Etapas 1 y 2 CERRADAS (D70 · D71) · 🔒 D72 Modelo A · 🔒 **Etapa 3 (D73) CERRADA: los 3 escenarios aprobados E2E y datos de Sandbox purgados** · 🔒 D74 (cadencia) | **Modelo (D72):** Flow = bóveda de tarjeta + `customer/charge`; Medusa dueño de la cadencia. La capa nativa de D71 sigue 💤 **DORMIDA**. **Etapa 3 (D73) — validación E2E real contra Sandbox usando el backend desplegado:** aparecieron **9 defectos que ninguna lectura de doc podía revelar**, 3 con costo real — **se cobraba solo el despacho** ($3.990 de $29.500: `cart.total` se calcula sobre las relaciones CARGADAS y la corrección de D58 nunca cruzó a la ruta de D59), **doble cobro** por callbacks concurrentes sin lock, y **un problema de Flow castigando al cliente** (cuota de Sandbox agotada ⇒ `past_due` + correo de cobro fallido). Corregidos y desplegados: monto, lock por carrito, pertenencia del token, orden de renovación pagada + con stock reservado, guardia de conciliación de monto, y la corrección de fondo **F6+F9** (`lookupFlowStatusByCommerceId` discriminado + `failureKind` + desenlaces `deferred`/`unverified`: *"no pude"* deja de significar *"no pagó"*). ⚠️ **Corrige una premisa de D72:** Flow **NO** deduplica `customer/charge` por `commerceOrder` (medido) → la única protección es la nuestra; el sufijo `-a${attempt}` se eliminó por la razón inversa a la que decía D72. **LOS TRES ESCENARIOS APROBADOS** (compra única de suscrito por el rail normal · 1ª compra suscrita con Customer reutilizado y suscripción persistida · **renovación** con orden Paid + Allocated, cargo único, cadencia correcta y correo entregado — F7 y F8 verificados por fin). **Al revalidar aparecieron 2 defectos más, ambos ya corregidos y verificados:** **#10**, un cobro aplazado dejaba la suscripción **trabada para siempre** —activa, vencida e invisible en el Admin— porque toda consulta posterior sobre una referencia que Flow nunca registró se leía como "no pude preguntar"; era un defecto **de la propia corrección de D73**, y se cierra con un tercer desenlace `not_found` (Flow responde `400 "Transaction not found"`, **medido**: corrige otra premisa de D73). El tipo discriminado destapó **el mismo bug en la ruta del alta**. Y **#11 → D74**: la cadencia se ancla en `max(fecha_pactada, ahora)`, para que una suscripción muy atrasada no se ponga al día con varios cobros y varios sacos seguidos. **Datos de Sandbox PURGADOS** de la BD de producción (411 filas; catálogo e inventario intactos, verificados por una guarda automática; el admin sobrevivió a un filtro que casi lo borra —su credencial tiene `user_id` y `customer_id` en la misma fila—). El cobro sigue **APAGADO** (`SUBSCRIPTION_CHARGES_ENABLED` ausente). **SIGUIENTE:** cerrar el checklist de `TODO.md` Frente 1b antes de devolver `FLOW_*` a Producción (el `code` de Flow, la cuota en Producción, despacho gratis a suscriptores, unificar `saved_card`↔`flow_customer`, copy y entregabilidad del correo, curación del arnés). | **D73** · **D74** · D72 · D71 · D70 · `TODO.md` Frente 1b · `API.md §14/§15/§16` |
| **Terceros** | 🟢 email + Resend **EN VIVO** (D49) · **Flow (D58) implementado, pendiente llaves+deploy** | Emails reales: dominio verificado + `RESEND_FROM` con nombre + `STOREFRONT_URL` apex en Railway (bienvenida verificada E2E). **Correos del ciclo de suscripción añadidos (D57·R5): pendiente deploy.** **Pago con Flow (D58) implementado y verificado (build+tsc+migración+firma);** falta E2E en sandbox + setear `FLOW_*` en Railway (`FLOW_API_URL=https://www.flow.cl/api` para prod). El pago **recurrente** de suscripciones sigue diferido (D55) | D45 · D49 · D57 · D58 · D30 |
| **SEO & Tracking** | 🟢 **EN VIVO** (D46/D30/D53): GTM + GA4 + **Meta Pixel** midiendo (Pixel verificado en Test Events 2026-07-23: PageView/AddToCart/InitiateCheckout) | Falta **Search Console** (verificar `tumanada.cl` con GTM + enviar sitemap) y, si hay campañas, **Google Ads** dentro de GTM. Del Pixel: spot-check de `Purchase` en una compra de prueba. Menor: base canónica del sitemap (`tumanada.cl` vs `www`) | D46 · D30 · D53 |
| **Funnel F5 — momento de registro** | 🟢 **DECIDIDO + implementado, APAGADO** (D65) · ⬜ pendiente **E2E en staging** | Auto-crear cuenta post-compra de invitado + email "Define tu contraseña" **diferido ~2 h** (job `send-account-activations`, token fresco; no compite con la confirmación). **GATEADO `AUTO_ACCOUNT_ENABLED=false`** → encender tras E2E. Fast-follow: copy del registro cuando el correo ya fue auto-creado | `FUNNEL_TARGET.md §1.6` · D65 |
| **Validación UI del Completion Pass (D41)** | ⬜ implementado, sin smoke manual | Carlos recorre: foto de mascota, /cuenta, /comenzar móvil, landing (el dashboard ya fue rediseñado y validado en D42) | D41 |
| **Pantalla de recomendación F4 — reconciliación (D62)** | 🟢 **implementada, smoke Playwright 16/16 (3 escenarios)** · ⬜ pendiente validación visual de Carlos | Recorrer con la app levantada: card + suscripción (`PlanManadaCard`) + compra única · alternativas en línea (elegir rearma el plan) · "ya come otra marca" → buscador · aviso de sobrepeso · móvil sin cortes | D62 · D44 · `FUNNEL_TARGET.md` |
| **Backoffice — carga de catálogo (D50 · D67)** | 🟢 widget "Formatos" (local) · **carga masiva por CSV EN VIVO en prod** | **Catálogo v3 cargado a producción (D67):** 89 productos de alimento / 172 variantes **publicados** vía `import-products.ts` (`IMPORT_CSV=…/products_v3.csv`, `medusa exec` in-container), con **multi-etapa real** (`stage` array). El importador acepta multi-etapa/multi-especie + N `category_ids` (compat con valor único). `subscribable=FALSE`/sin `kcal_per_kg` por decisión consciente. Pendiente: deploy del widget "Formatos" del Admin + subir packshots | D50 · D67 · `API.md §12` |
| **Packshots de producto** | 🟢 **render resuelto (D52)** · ⬜ falta subir fotos reales (hoy emoji) | El front **normaliza cualquier packshot** (fondo blanco o transparente) vía `/api/packshot` → encuadre grande y consistente **sin editar assets**. Ya **no** hace falta recortar a mano: basta subir la foto por el Admin. Productos sin foto siguen en emoji hasta que se suban | D52 · `public/fotos/README.md` |
| **Foto de mascota → blob definitivo** | ⬜ hoy andamio local (localStorage) | Con la estrategia de storage app-wide: swap interno de `setPetPhoto` + PATCH `avatar_url` | D41 · `API.md §9` |
| **Rama `cristobal-cambios` — rescate selectivo (D54)** | 🟡 **rescate parcial hecho** | **Bloque 1** onboarding a 2 pasos (`755803c`) ✅ · **Bloque 2** `/cuenta` con tabs + perfil humano (`b4684d9`) ✅ · **Bloque 3** suscripción-PDP → **escalado al chat de suscripciones** (solo lo visual; la card ya vive oculta) · **Servicios** descartados por ahora. **Pantalla de recomendación (D62): diseño reconciliado** (visual de Cristóbal + suscripción real de `main`) ✅. Reimplementado sobre `main` (no merge/cherry-pick); **D42/D29/D39 intactos**. Rama origen preservada en `origin/cristobal-cambios` (HEAD `3f468f3`) | D54 · D62 |
| **Suscripciones recurrentes (moat)** | 🟢 **D56 completo · refinamiento D57 (R1–R5) COMPLETO y validado** | **Punto 1 (D55) E2E:** card **Plan Manada** → carrito → checkout → orden → **suscripción creada** (`subscription-created.ts`) → `GET /store/subscriptions`. Módulo `subscription` + links `customer`/`pet`; **precio suscrito cobrado desde la 1ra compra** (`POST /store/carts/:id/subscription-items`, precio custom; compra única intacta). El **pago del checkout** (también el de la 1ra compra suscrita) va ahora por **Flow** (D58); el **cobro recurrente** sigue diferido (D55). **Rediseño UX (D56) COMPLETO A→B→C→D** (frontend primero · reuso máx · sin pantallas nuevas · responsive): **A** card controlada 100% reactiva (fix `subscriptionPrice` per-variante = mostrado=cobrado) + catálogo invierte CTAs · **B** hoja de confirmación post-suscribir (`SubscribeFlowProvider` reusando el `Dialog`) · **C** Home/`PetStatusCard` evoluciona por estado (`SubscriptionProvider`; suscrito = centro del plan / no = nudge) · **D** gestión (`PlanManageSheet` reusada Home+/cuenta via `PlanManageProvider` + `PATCH /store/subscriptions/:id`: frecuencia/saltar/pausar/reanudar/cancelar; único con backend, verificado E2E). **Refinamiento D57 (R1–R4) COMPLETO y validado en navegador** (frontend puro): (1) Home con plan **pausado** = plan (selector `subscriptionForProduct`; CTA "Reanudar plan" abre la Sheet, ya **no** invita a la PDP); (2) **confirmaciones** antes de Pausar/Saltar (`ConfirmInline`); (3) **reanudar** con momento de éxito in-sheet (reusa `Dialog`+motion); (4) Home con plan activo = **"Tarjeta miembro" Pino+Oro** (scope `[data-premium]` en `globals.css` + CTA dorado + nombre en Oro + "Ahorras $X"). **R5 (backend) COMPLETO y validado:** **eventos de dominio** `subscription.created/paused/resumed/cancelled/skipped` (1er patrón de eventos propios del proyecto; `created` en su subscriber, el resto derivados en el `PATCH` comparando estado previo — frecuencia sola no emite) + **5 correos de ciclo** sobre Resend (patrón D45: 5 `.tsx` + registro + helper único `src/lib/subscription-email.ts` + 5 subscribers), honestos (sin prometer cobro/despacho automático; "Plan activo" explica cómo gestionar, no repite la compra). Contrato en `API.md §11.3`/`§13.2`; verificado con `medusa build`; correos previsualizados (React Email) y validados por Carlos; **pendiente deploy a Railway** (con D50). **SIGUIENTE = revisión E2E del flujo de suscripción** (consistencia UX/copy — candidata #1: card "Entregas automáticas · Activo" vs. honestidad de los correos). Follow-ups D56 previos: equilibrio visual CTAs; multi-formato "Suscribirme" desde catálogo usa formato primario; sostener fuente única. **Moat restante (frente D55):** scheduler → **pago recurrente real (go aparte)** → estados de fallo+emails | **D57** · D56 · D55 · D22 · D29 |
| **Post-tracción** | ⬜ diferido | SII/boleta, courier, WhatsApp (la suscripción recurrente tiene su propio frente arriba) | D21/D22 |

## Claves del código (para no re-derivar)

- **Capa Medusa del front:** `apps/web/src/lib/medusa/` — `client.ts` (SDK, JWT en localStorage/SSR nostore) · `products.ts`/`map-product.ts` (catálogo; `SUBSCRIPTIONS_ENABLED`; `getProductByHandle` con `React.cache`) · **`catalog-cache.ts`** (`server-only`; envolturas `unstable_cache` que usan SOLO las páginas ISR, D59) · `cart.ts` · `checkout.ts` · `auth.ts`/`account.ts` · `shipping.ts` · `pets.ts` (mapper `StorePet→Pet`).
- **Providers:** `components/providers/` — `session-provider` (sesión JWT persistente) · `pet-provider` (hidrata `/store/pets` al login, empuja mascotas de invitado, `updatePet`/`assignFood` optimistas) · `cart-provider` (cart_id en localStorage). Coordinador: `hooks/use-auth-actions.ts` (login/registro/logout + `transferCart`).
- **Backend custom:** `apps/backend/src/modules/pet` · `src/api/store/pets` (+ validators zod) · `src/subscribers/{password-reset,food-purchased}.ts` · `src/api/middlewares.ts` (`subscription_price`) · `src/lib/shipping.ts` + `src/scripts/{seed,setup-free-shipping}.ts`.
- **Pago Flow (D58):** `src/modules/flow-payment` (modelo `flow_payment`) · `src/lib/flow.ts` (firma HMAC + `payment/create`/`getStatus`) · `src/lib/flow-settle.ts` (conciliación idempotente) · `src/api/store/carts/[id]/flow-payment` + `src/api/flow/{confirmation,return}`. Front: `apps/web/src/lib/medusa/flow.ts` + `app/checkout/*`. Contrato: `API.md §14`.
- **Funnel/perfil:** `app/comenzar/*` (wizard + recomendación server-hydrated) · `lib/recommend.ts` (puro, recibe `products`) · `lib/anticipation.ts` · `app/(tienda)/cuenta/mascotas/*` + `components/pet/{pet-edit-dialog,food-selector-dialog,pet-tag}.tsx`.
- **Identidad del header (D61):** `components/pet/account-menu.tsx` (menú único cuenta⊕mascota, Radix) montado en `components/layout/header.tsx`; el logo (`logo.tsx`) sigue = Inicio. Reemplaza al desaparecido `pet-switcher.tsx`.
- **Home logueada (D42):** `app/dashboard-view.tsx` (centro de control) · `components/pet/{pet-status-card,pet-action-grid}.tsx` · `components/commerce/quick-buy-card.tsx`.
- ⚠️ Antes de tocar `apps/web`: leer `apps/web/AGENTS.md` + docs de Next 16 en `node_modules/next/dist/docs` (cambios de ruptura). Component-system-first: mapear a tipos/componentes existentes, no reescribir.

## Modo de trabajo vigente

- **Bloques pequeños:** un bloque → validado por Carlos → un commit. **Frontend primero; la integración backend es una pasada separada** (regla de oro de FUNNEL/PET_EXPERIENCE).
- **Todo dato estructurado se edita con el mismo patrón del funnel** (chips/selector/buscador, nunca texto libre) — regla vinculante D38.
- **Comprar ≠ definir qué come** (D39): la tienda vende; el perfil define; el puente es explícito (toast de un tap).
- En decisiones no estratégicas: tomar la opción claramente superior y documentar rationale; las estratégicas esperan a Carlos.
- Al cerrar un hito: seguir el barrido de punteros de `PROJECT_HEALTH_REPORT.md §5` (CURRENT_STATE se reescribe, no se apila).
