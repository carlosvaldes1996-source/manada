# CURRENT STATE — Dónde estamos AHORA

> **📋 Metadata**
> | Campo | Valor |
> |---|---|
> | **Purpose** | Foto del estado actual del proyecto y desde dónde continuar. Lo primero que se actualiza cada sesión. |
> | **Owner** | Carlos (fundador) · Claude |
> | **Status** | 🟢 Vivo |
> | **Last Updated** | 2026-07-06 |
> | **Depends On** | DECISIONS.md, ROADMAP.md, AUDIT_UI_UX.md |
> | **Supersedes** | — |
> | **Source of Truth** | ✅ del *estado actual y el siguiente paso*. |

> *Actualizado: 2026-07-08 (**D29: Endurecimiento pre-lanzamiento ✅** — auditoría de CTO + 7 correcciones de código de bajo riesgo: suscripción **atenuada** [`SUBSCRIPTIONS_ENABLED=false`, se vende como compra única], total del checkout que refleja la regla real de envío del backend, copy sin promesas de suscripción [carrito/términos], menú móvil sin enlaces 404, `/dev/*` gateado en prod, cabeceras HTTP de seguridad, `error.tsx`/`not-found.tsx` de marca. Solo `apps/web`, backend intacto, reversible. Resta = infra + terceros. Base: **D28: Etapa B — tienda coherente y honesta ✅** — buscador real [`q` Store API], cross-sell real, **regla única de envío** [backend: opción $3.990 + promoción automática de envío gratis ≥ $30.000, expuesta en `/store/shipping-policy`] y auditoría de copy [sin promesas de Webpay/SII/pago protegido; reseñas y ratings ocultos; sin fechas de envío inventadas]. **El flujo propio del MVP queda CERRADO** → solo restan terceros [Mercado Pago primero] + infra. Base: D27 [Vercel]/D26 [Etapa A]/D25/D24/D23/D22)*

## Fase activa
**Fase 5 — MVP (D22, MVP-first).** La arquitectura base quedó **definida y aprobada** (D20 estructura + D21 Medusa v2) y se **cierra el refinamiento**: cada decisión se evalúa por *¿acerca o retrasa el lanzamiento?*, con la **alternativa manual** por defecto (envíos y boleta manuales; sin courier/SII/WhatsApp hasta tracción). El objetivo inmediato es **arrancar el backend** (scaffold de Medusa en `apps/backend`) con el modelo de datos mínimo del checkout. El moat (perfil/suscripción/anticipación) se mantiene como visión y se construye tras el MVP transaccional. *— Contexto arquitectónico previo (sigue vigente):* Segundo hito ✅ **D21 (2026-07-06): stack backend = Medusa.js v2** en `apps/backend`, bajo el **principio de producto "e-commerce primero"** (perfil/suscripciones/recomendaciones/anticipación mejoran la compra; no somos plataforma de gestión de mascotas) y los criterios de Carlos: velocidad de MVP · simplicidad para fundador único · bajo mantenimiento · escalar sin sobreingeniería. El moat se construye como **módulos custom** (`pet-profile`, `subscription`, `anticipation`) sin fork del core; Webpay será payment provider custom. Primer hito ✅ **D20 (2026-07-06): estructura física del repositorio** — monorepo pnpm workspaces; el frontend se movió intacto de `web/` a **`apps/web`** (`@manada/web`), **`apps/backend` queda reservado** (`src/` + `docs/` + README, sin código hasta validar stack) y `packages/shared` **no se crea** hasta el primer contrato compartido aprobado en `API.md`. **Reglas arquitectónicas permanentes en `ARCHITECTURE.md §2`** (web sin lógica de negocio ni DB; backend solo en `apps/backend`; comunicación solo vía `API.md`; contrato primero, código después; nuevas apps/paquetes solo con aprobación). Fases 0–2 ✅ completas. **Fase 3 funcionalmente completa** (etapas 1–3 ✅ + Polish lote 1 ✅); el **Polish 3.4 restante queda ⏸ en pausa** (D19): su track fotográfico está bloqueado por assets (U090) y varios ítems necesitan datos reales que recién existirán con backend.

## Último avance
- 🟢 **Fase 5 · Endurecimiento pre-lanzamiento (2026-07-08, D29):** tras una **auditoría de lanzamiento** (rol CTO) que reclasificó los hallazgos en BLOQUEANTE/IMPORTANTE/POST y los validó con evidencia (descartando 2 falsos positivos), se implementaron **solo las 7 correcciones de código pequeñas y reversibles** (los bloqueantes de infra/terceros quedan fuera): (1) **suscripción atenuada** — interruptor único `SUBSCRIPTIONS_ENABLED=false` en `lib/medusa/map-product.ts` apaga en cascada toda la UI de suscripción (caja PDP, precio con descuento, badge, CTA, toast) porque estaba gateada por `product.subscribable`; el catálogo se vende como **compra única** (el backend sigue calculando `subscription_price`, contrato intacto); (2) **total del checkout coherente** — `checkout/page.tsx` consume `getShippingPolicy()` y aplica la regla real (gratis sobre umbral; si no, costo de la opción) → coincide con la orden; (3) **copy sin suscripción** en carrito y Términos; (4) **menú móvil sin 404** (`LIVE_ACCOUNT_HREFS` en `config/nav.ts`); (5) **`/dev/*` gateado** en prod (`app/dev/layout.tsx` → `notFound()` si `NODE_ENV==="production"`); (6) **`error.tsx`/`not-found.tsx` de marca**; (7) **cabeceras HTTP de seguridad** en `next.config.ts` (sin CSP). **Fuera de alcance (decisión):** el copy de visión/marca (hero landing D28, `/anticipacion`, footer, onboarding, `/bienvenida`) se conserva; con la suscripción apagada ya no queda promesa transaccional viva. **Verificado:** `tsc`+`eslint`+`build` (25 rutas) ✅ y smoke en `next start` (prod, backend local): PDP sin suscripción + precio base $24.990; `/carrito` y `/terminos` limpios; `/dev/*`→404; ruta inexistente→404 de marca; cabeceras presentes sin `X-Powered-By`; `shipping-policy` {3990,30000}. Detalle en **D29**.
- 🟢 **Fase 5 · Etapa B — Tienda coherente y honesta (2026-07-07, D28):** se eliminaron las últimas dependencias demo del **flujo propio** y se cerró la coherencia de la tienda. **Buscador real:** `HeaderSearch` → `/buscar?q=` sobre el `q` nativo de la Store API (`searchProducts`). **Regla única de envío (backend = fuente de verdad):** opción Estándar $3.990 + **promoción automática nativa** de envío gratis ≥ $30.000 (`is_automatic`, script idempotente `setup-free-shipping.ts`, sin reseed → la **orden real** queda con envío $0 cuando corresponde), expuesta en **`GET /store/shipping-policy`** y consumida por el front (`getShippingPolicy`); se eliminó el umbral duplicado del front. **Cross-sell real** en el carrito (`listProducts`). **Despacho honesto:** se quitó la fecha/comuna inventadas por tarjeta (prop `shipping` eliminado de `ProductCard`/`Grid`/`Rail`); la política real se muestra con `ShippingPolicyNote` (PDP + carrito). **Reseñas y ratings ocultos** (decisión de Carlos; no hay sistema real). **Auditoría de copy:** fuera "boleta SII"/"pago protegido"/"pago seguro"/fechas de envío falsas → copy alineado al MVP real (transferencia manual + despacho coordinado + costo real + envío gratis sobre umbral). El **hero de la landing** (anticipación demo "Toby") se mantiene **tal cual** por decisión de marca (preview del concepto). **Verificado:** `tsc`+`eslint`+`build` (25 rutas) ✅ y smoke en vivo (política 3990/30000; `q=royal`→1, `q=zzznope`→0; **orden bajo umbral → $3.990**, **orden sobre umbral → $0**). **El código propio de Manada queda cerrado; solo restan terceros.**
- 🟢 **Fase 5 · Etapa A — Cuentas y sesión reales (2026-07-06, D26):** la sesión demo en memoria se reemplazó por **cuentas reales de cliente**, todo con capacidades **nativas de Medusa v2** (Auth + Customer + Order Modules; cero terceros, cero módulos custom). **Registro/login/logout** emailpass (`auth.register`→`customer.create`→`auth.login`), **sesión persistente** (SDK con `auth:{type:"jwt"}` → token en `localStorage`, re-hidratación con `customer.retrieve()` al montar; SSR seguro con `nostore`), **recuperación de contraseña** (`auth.resetPassword` + `auth.updateProvider`, pantallas `/recuperar` y `/recuperar/nueva`; entrega del token vía **subscriber** del backend `auth.password_reset` — dev: log; prod: email diferido). **Transferencia automática del carrito invitado → cliente** al iniciar sesión (`cart.transferCart`), **historial real de pedidos** (`/cuenta/pedidos`, `order.list`) y **direcciones reales** (`/cuenta/direcciones`, CRUD nativo de `customer.addresses`). **La compra de invitado quedó idéntica** (solo se prellena nombre/correo si hay sesión). Se eliminó el login demo (Carlos+Toby) y el `signUp` en memoria; las fixtures del moat (Toby/anticipación) se mantienen (diferidas). Capas: `apps/web/src/lib/medusa/{auth,account}.ts` + `cart.transferCartToCustomer` + `session-provider`/`use-auth-actions` reescritos. **Verificado punta a punta:** `tsc`+`eslint`+`build` (28 rutas) ✅ y flujo en vivo con el mismo js-sdk → registro→login→me→transferCart→direcciones→orden ligada al cliente→historial→logout→re-login→reset→login inválido rechazado, todo verde. **Siguiente: Etapa B (tienda coherente y honesta).**
- 🟢 **Fase 5 · Etapa 3 — Compra de punta a punta (2026-07-06, D24):** una persona puede comprar de principio a fin. **Carrito real de Medusa** (el `cart-provider` se reescribió sobre la Store API; `cart_id` persistido en `localStorage`; superficie pública preservada → cero cambios en los componentes de carrito). **Checkout real**: invitado (email + dirección) → **shipping options reales** → método de despacho → **sesión de pago manual** → **`cart.complete()` = orden real**. Página `/checkout/confirmacion`. Capas: `apps/web/src/lib/medusa/cart.ts` + `checkout.ts`. **Todo nativo de Medusa** (backend fuente de verdad; el front solo orquesta). **Verificado punta a punta**: `tsc`+`eslint`+`build` ✅; flujo ejecutado con el mismo js-sdk del frontend y por Store API cruda → **órdenes reales #1 y #2** creadas, **stock reservado** (NexGard→999, Acana→998), pago `authorized`, órdenes `pending` en la BD/Admin listas para despacho manual. **Compra única** (la suscripción recurrente es el moat, post-MVP). **Sin MP/courier/SII/WhatsApp** (D22). **Siguiente:** transferencia carrito→cliente al login + **Mercado Pago Checkout Pro**.
- 🟢 **Fase 5 · Etapa 2 — Catálogo conectado al backend real (2026-07-06, D23):** el **backend es la fuente de verdad de todo el catálogo**. Seed enriquecido con **metadata Manada** (`brand`, `species`, `stage`, `subscribable`, `subscription_discount_percentage`, `rating`, `review_count`) + campos nativos (SKU, formato, stock, categorías, imágenes) — **administrable desde el Admin de Medusa sin tocar código**. **Un solo precio base**; el **precio de suscripción lo calcula el backend** (campo `subscription_price` vía middleware, redondeo CLP idéntico al front) y el frontend solo lo consume. Mapper reescrito **sin heurísticas de nombre**; lógica de PLP pura en `lib/catalog.ts` (facetas de marca derivadas de los productos). **Home, PLP y PDP** hidratan desde la Store API (`force-dynamic`), misma experiencia visual, sin romper componentes. Verificado: `tsc` (web+backend) + `eslint` + `next build` + smoke en vivo (Home ids reales; PLP filtra por especie/etapa/categoría/marca desde metadata; PDP con precio base + suscripción del backend). **Pendiente:** carrito/checkout y dashboard personalizado siguen en demo. Contratos en `DATABASE.md §5` + `API.md §5`.
- 🟢 **Fase 5 · Etapa 1 — Fundación de datos (2026-07-06):** capa `apps/web/src/lib/medusa/` — cliente `@medusajs/js-sdk` + `.env.local` (URL backend + publishable key) + `listProducts`/`getProductByHandle` + mapper `StoreProduct → Product`. Ruta de diagnóstico `/dev/medusa`. Verificado contra el backend real.
- 🟢 **Fase 5 · Backend MVP arrancado y verificado (2026-07-06, D22):** **Medusa.js v2** scaffolded en `apps/backend` (starter bare `@medusajs/medusa` 2.16.0, integrado al workspace pnpm; NO el template DTC/Turborepo). Postgres local `medusa_manada`, migraciones ✅, admin `admin@tumanada.cl` ✅. **Seed Chile** (`src/scripts/seed.ts`): tienda **CLP**, región **Chile**, despacho manual (Estándar $3.990/Express $5.990), pago **manual `pp_system_default`** (transferencia), sales channel **Manada Web** + publishable key (1↔1), **6 productos** alineados con `apps/web/.../catalog.ts`. **Checkout verificado punta a punta vía Store API:** carrito → línea → dirección → despacho → pago manual → **`complete` → orden #1 registrada ($28.980 CLP, dir. almacenada)**. Setup reproducible en `apps/backend/DEV.md`. Config workspace: root `.npmrc` (hoist Medusa) + `onlyBuiltDependencies` en `pnpm-workspace.yaml`. **Pendiente:** decidir medio de pago para lanzar (Webpay vs transferencia-manual) y **conectar `apps/web` al backend real**.
- ✅ **Fase 1 (Identidad de marca)** completa (D8·D9·D10·D11): nombre **Manada · `tumanada.cl`**, dirección visual, logo "huella-manada" y **Brand & Visual Design System** completo (DESIGN_SYSTEM.md).
- ✅ **Fase 2 (UX)** completa: arquitectura de información, journeys, wireframes (UX.md) + **prototipo HTML estático** (`prototype/`, 6 páginas con chrome en Web Components) + **plan del sistema de componentes** (D12 → FRONTEND_ARCHITECTURE.md).
- ✅ **Fase 3 · Etapa 1 — Fundaciones (D13):** scaffold Next.js en **`web/`**. Estructura, routing, providers, design tokens, tema y tipografías listos y verificados (build + type-check + lint + smoke-test en verde).
- ✅ **Auditoría crítica de UI/UX → backlog priorizado (D14):** `AUDIT_UI_UX.md` (108 ítems `U001`–`U122`). **Fuente de verdad de mejoras de frontend.**
- ✅ **Fase 3 · Etapa 2 — Component Library (D15):** **librería completa de ~70 componentes reutilizables** en `components/{ui,layout,commerce,pet}` + `hooks/` + `lib/`, documentada en **COMPONENT_LIBRARY.md** y con **styleguide navegable en `/dev/components`**. Resuelve los 3 P0 de Fase 3.2 de AUDIT_UI_UX.md (+ mayoría de P1). `tsc` + `eslint` + `next build` (10 rutas) + smoke-test HTTP 200 ✅.
- ✅ **Fase 3 · Etapa 3 — Pantallas** ensambladas con la librería: Home, Categoría, Producto, Carrito, Checkout, Mi Cuenta + páginas de contenido (nosotros, ayuda, despacho, devoluciones, términos, privacidad, anticipación).
- 🟢 **Fase 3.3B — New User Experience & Activation Flow (D16):** embudo de activación completo para el visitante nuevo — **Landing → alta de mascota conversacional → recomendación personalizada → registro → primer carrito → checkout → bienvenida**. Modelo de **sesión** (`SessionProvider`/`useAuthActions`); providers de mascota/carrito arrancan **vacíos**; `/` decide Landing vs panel según sesión (U041/U058). `tsc` + `eslint` + `next build` (22 rutas) + smoke-test HTTP 200 ✅. *(Revisado y cerrado por D17.)*
- 🟢 **Fase 3.4 — Polish · lote 1 (track no-fotográfico) ✅ (2026-07-05):** el fundador aprobó arrancar el polish por lo que no depende de imágenes (las fotos IA generadas con ChatGPT se probarán después, sin quemar tokens ahora). **10 ítems cerrados:** Fraunces como variable font con eje óptico por nivel (U009) · escala tipográfica sin solape 28–36/36–48/44–60 (U010) · `neutral-500` oscurecido a #6F665A para AA en los 3 fondos (U028) · contraste del botón miel verificado (U029) · **política de redondeo CLP** (piso a múltiplo de $10; `subscriptionPrice()`/`roundCLP()` en `lib/format.ts` reemplazan 5 cálculos duplicados) (U066) · barra de anticipación con llenado calmado `animateIn` (U085) · pulso-alerta de la cápsula eliminado (U087) · "¿Por qué te lo decimos?" con afordancia de botón (U088) · banda oscura de la landing como pieza editorial inset (U089) · U101 verificado (deuda solo del prototipo). `tsc` + `eslint` + `next build` (22 rutas) + smoke ✅.
- ✅ **Fase 4 · Stack backend decidido (D21, 2026-07-06): Medusa.js v2** — elegido por Carlos con criterios explícitos (velocidad MVP > simplicidad fundador único > bajo mantenimiento > escala sin sobreingeniería; regla 80–90%). Validado contra Vendure/Saleor/custom; extensión vía módulos custom (`pet-profile`/`subscription`/`anticipation`) + payment provider Webpay custom. Rationale completo y riesgos en D21; `ARCHITECTURE.md §3` actualizado.
- ✅ **Fase 4 · Estructura física del repositorio (D20, 2026-07-06):** monorepo pnpm — `git mv web apps/web` (app intacta, package → `@manada/web`), workspace raíz (`apps/*` + `packages/*`), lockfile único en raíz, `apps/backend` **reservado sin código** y **reglas arquitectónicas permanentes** en `ARCHITECTURE.md §2`. Verificado idéntico: `tsc` + `eslint` + `next build` (22 rutas) + smoke HTTP 200 (incl. PDP/PLP dinámicas) ✅.
- ✅ **Fase 3.3 — Product Experience CERRADA (D17, 2026-07-03):** revisión visual manual completa con mejoras aplicadas en dos rondas: **sistema** (token `--radius-pill` faltante, cursor pointer global por preflight de Tailwind v4, alturas uniformes de ProductCard, diálogo real de reagendo con fecha programada, "Agregar mascota" + lógica multi-mascota) y **modelo invitado** — *"e-commerce como piso, perfil como camino destacado"*: landing-vitrina con riel y tienda navegable anónima, header marketing con buscador/nav/carrito, **checkout de invitado punta a punta**, gate honesto de suscripción (cuenta solo si hay suscripción, anunciado en el botón) y registro valor-primero **post-compra** en `/bienvenida` (email prellenado). `SessionProvider` gana estado `guest`. **AUDIT Fase 3.3: 27/33 Hechos** (U066 → Polish; U070 → Futura). `tsc` + `eslint` + `next build` (22 rutas) + smoke ✅.

## En la mesa ahora mismo (`apps/web/`)
- **Stack:** Next.js 16 (App Router, Turbopack) · React 19 · TS 5.9 · **Tailwind v4 (CSS-first, `@theme`)** · **Radix UI** (primitivas a11y, 16 paquetes) · framer-motion 12 · lucide-react.
- **Componentes (Etapa 2):** `ui/` primitivas (layout, botones, badges/chips, tarjetas, datos, formularios, feedback, navegación) · `layout/` chrome (AppShell/Header/Navbar/MegaMenu/Footer/BottomNav/MobileNav/Logo) · `commerce/` (ProductCard/Grid/Rail, badges, SubscriptionBox, FiltersPanel, CartItem/Drawer, OrderSummary, Coupon, Shipping/PaymentMethod, CheckoutStepper) · `pet/` (PetSwitcher, AnticipationCapsule, PetProfileHeader/Card/EditCard, FeedingSchedule, RecommendationCard). Importables por barrels (`@/components/ui` etc.).
- **Soporte:** `hooks/` (useDisclosure, useMediaQuery/useBreakpoint, usePrefersReducedMotion, useSubscription) · `lib/` (format [+`effectiveSubscriptionPrice`], motion, anticipation, icons, **`medusa/`** [cliente SDK + `listProducts`/`getProductByHandle` + mapper], **`catalog.ts`** [taxonomía + filtro/facetas puros], `data/catalog` [solo demo restante]) · datos demo (Carlos + Toby) para carrito/checkout/dashboard.
- **Tokens:** todo el design system en `globals.css` (`@theme`). Styleguides vivos en `/dev/tokens` y `/dev/components`.
- **A11y:** Radix para overlays/controles (foco-trap, teclado, ARIA); foco visible; color+ícono/texto; `prefers-reduced-motion`.
- **Pendiente operativo (no bloquea):** registrar `tumanada.cl` + handles; verificar "Manada" en INAPI; ejecutar logo en vector; **fotografía real (U090)** condiciona el polish.

## Siguiente paso
**▶️ Solo terceros + infra de lanzamiento (el flujo propio del MVP quedó CERRADO en D28).** El código propio de Manada —catálogo, carrito, checkout→orden, cuentas/sesión, buscador y envío— es 100% real sobre Medusa. Lo que resta para lanzar **no es código propio nuevo**: (1) **Mercado Pago** Checkout Pro (pago automático; hoy = transferencia manual); (2) **email transaccional** (entrega del enlace de recuperación + confirmaciones/boleta; hoy manual/log); (3) **infra**: desplegar el backend (Railway/Postgres/Redis) + setear las 2 env vars en Vercel (D27) + dominio + gatear `/dev/*`; (4) post-tracción: SII/courier/WhatsApp automáticos y el **moat** (perfil/anticipación/recomendación/suscripción recurrente). *Registro histórico de cómo se llegó aquí:*
1. ✅ **Fundación (Etapa 1):** SDK Medusa (`@medusajs/js-sdk`) en `apps/web` + `.env.local` + capa `lib/medusa/` que mapea producto Medusa → tipo `Product`.
2. ✅ **Catálogo (Etapa 2, D23):** home/PLP (`categoria/[slug]`) y PDP (`producto/[slug]`) con productos reales por la Store API; catálogo Medusa-native + metadata administrable + `subscription_price` calculado por el backend.
3. ✅ **Carrito + Checkout → orden (Etapa 3, D24):** carrito real de Medusa + persistencia; checkout invitado → dirección → despacho → **pago manual** → **orden real** (stock reservado, visible en el Admin). Compra única.
4. ✅ **Auditoría de MVP + plan de cierre (D25):** clasificado todo lo demo; alcance fijado (cuentas SÍ, moat diferido). Cierre del **flujo propio** en 2 etapas ↓.
5. ✅ **Cuentas y sesión reales (Etapa A, D26):** auth nativo de Medusa (registro/login/logout/recuperación, sesión persistente JWT), `transferCart` carrito→cliente al login, **Pedidos** (historial real) + **Direcciones** (CRUD nativo). Reemplazado el `session-provider`/`use-auth-actions` demo; cableados `ingresar`/`crear-cuenta`/`recuperar`/`bienvenida`. Compra de invitado intacta. Verificado punta a punta.

**Plan de cierre del flujo propio (D25) — ✅ COMPLETO:**
- ✅ **Etapa A · Cuentas y sesión reales (D26):** hecha y verificada. Cierra G1 #1-4 de D25.
- ✅ **Etapa B · Tienda coherente y honesta (D28):** buscador real (`q`), cross-sell real, **regla única de envío** (backend: opción $3.990 + promoción automática de envío gratis ≥ $30.000, expuesta en `/store/shipping-policy`), **auditoría de copy** (sin Webpay/SII/"pago protegido"/fechas falsas), reseñas y ratings ocultos. Cierra G1 #5-7 + G2 de D25. **El flujo propio del MVP queda cerrado.**
- **▶️ Después = solo terceros + infra:** **Mercado Pago** (Checkout Pro; provider module + webhook + región + redirect/confirmación) → **email transaccional** (entrega del enlace de recuperación + confirmaciones/boleta), SII/couriers/WhatsApp (post-tracción); **infra de lanzamiento** (deploy del backend Railway/Postgres/Redis + env vars en Vercel [D27] + dominio + gatear `/dev`). **Moat** (perfil/anticipación/recomendación/suscripción recurrente) diferido (D22) — nota: la **UI de suscripción** existe pero agrega línea única; decidir antes del lanzamiento si se cablea o se atenúa su copy.
> Restricción: Next 16 tiene cambios de ruptura — leer `apps/web/AGENTS.md` + docs en `apps/web/node_modules/next/dist/docs` antes de tocar el front. Respetar la Component Library y los tipos existentes (mapear, no reescribir). **Diferido a Fases 6–7 (con tracción):** Webpay/courier/SII/WhatsApp, motor de anticipación completo, suscripción inteligente (el moat, D21, es posterior al MVP transaccional).

## En pausa (se retoma cuando existan las fotos)
**⏸ Polish 3.4 restante:**
- **Lote 2 no-fotográfico:** U086 (vuelo al carrito) · U100 (home con un solo clímax) · U096/U097 (confianza realzada) · U094 (deleite de cumpleaños) · U095 (tono salud) · U098/U099 (acento/texturas) · U104 (urgencia) · **U003** (color de suscripción — decisión de marca con Carlos pendiente).
- **Track fotográfico (bloqueado por assets):** U080/U081/U082/U084/U091/U092. Carlos generó **imágenes con ChatGPT para probar** (aún no integradas); existe además un **shot list detallado por pantalla** propuesto vía Claude-Chrome (hero, Nosotros, íconos de categoría, login/onboarding, banners PLP) — evaluar si se adopta como brief. La política definitiva IA-vs-fotografía-real (U090) **sigue sin decidirse**.

> **Para continuar en un chat nuevo:** prompt de `CLAUDE.md`. Claves del código: **auth real** en `apps/web/src/lib/medusa/{auth,account}.ts` (SDK nativo) + `components/providers/session-provider.tsx` (sesión persistente JWT, `isLoading`, `refresh`/`signOut`) + `hooks/use-auth-actions.ts` (coordina login/registro/logout + `transferCart`); pantallas `app/{ingresar,crear-cuenta,recuperar,recuperar/nueva,bienvenida}` y cuenta `app/(tienda)/cuenta/{pedidos,direcciones}`; subscriber de recuperación en `apps/backend/src/subscribers/password-reset.ts`. Embudo `comenzar` + vistas `landing-view`/`dashboard-view`, motor en `lib/recommend.ts`, anticipación **demo (moat diferido)** en `lib/demo-data.ts` (`TOBY_ANTICIPATION`, fuente única). Chrome del embudo = `layout/FunnelShell`; landing/tienda anónima = `AppShell variant="marketing"`.

## Modo de trabajo vigente
Incremental con calidad de producción: completar etapa → actualizar docs → **esperar aprobación** antes de la siguiente (mandato del brief de Fase 3). En decisiones no estratégicas, tomar la opción claramente superior y documentar rationale.
