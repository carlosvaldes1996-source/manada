# components/layout — Chrome de la app

Estructura de página. Consumen `components/ui`. Import: `@/components/layout`.

**Contenido:** AppShell, Header, Navbar, MegaMenu, MobileNav, Footer, BottomNav, Logo.

> Catálogo: `ai-context/COMPONENT_LIBRARY.md` §4. Styleguide: `/dev/components` (sección "Chrome").
